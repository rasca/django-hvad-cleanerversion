============
Installation
============

At the command line::

    $ easy_install djang-hvad-cleanerversion

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv djang-hvad-cleanerversion
    $ pip install djang-hvad-cleanerversion
